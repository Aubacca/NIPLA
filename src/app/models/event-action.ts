export interface EventAction {
  id: number;
  event: string;
  action: string;
  request: number;
  country: string;
  priority: string;
  primaryRequestType: string;
  phase: string;
  receiptDate: string;
  dueDate: string;
  user: string;
  role: string;
}
